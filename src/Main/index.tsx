import React from "react";
import type { WrapperProps } from "../components/@types";
import Page from '../components/Layout/Page'

export default ({ children }: WrapperProps)=> <Page>{children}</Page>
